{
  "name": "devtier",
  "version": "0.4.30",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "collect:users": "if [ -f .env.local ]; then tsx --env-file=.env.local scripts/collect-users.ts; else tsx scripts/collect-users.ts; fi",
    "collect:scores": "if [ -f .env.local ]; then tsx --env-file=.env.local scripts/collect-scores.ts; else tsx scripts/collect-scores.ts; fi",
    "recalc:tiers": "if [ -f .env.local ]; then tsx --env-file=.env.local scripts/recalc-tiers.ts; else tsx scripts/recalc-tiers.ts; fi",
    "batch:all": "npm run collect:users && npm run collect:scores && npm run recalc:tiers",
    "release": "release-it"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.2",
    "@supabase/supabase-js": "^2.105.1",
    "html-to-image": "^1.11.13",
    "html2canvas": "^1.4.1",
    "next": "16.2.4",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^11.0.0",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/pg": "^8.20.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.4",
    "pg": "^8.20.0",
    "release-it": "^20.0.1",
    "tailwindcss": "^4",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
